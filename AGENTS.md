## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout with `CONTEXT.md` at repo root and `docs/adr/` for decisions. See `docs/agents/domain.md`.
